---
import Component from "../components/Component.astro";
import Experience from "../components/experience/index.astro";
import { EXPERIENCE } from "../data/experience";
import { SOCIALS } from "../data/socials";
import Layout from "../layouts/Layout.astro";

import Skill from "../components/skills/index.astro";
import { SKILLS } from "../data/skills";
---

<Layout title=" Thomas Eduardo">
  <div class="relative hero z-10">
    <div class="mx-auto min-h-screen max-w-screen-2xl px-6 py-12 font-sans md:px-12 md:py-20 lg:px-24 lg:py-0 animate-fadeIn">
      <div class="lg:flex lg:justify-between lg:gap-4">
   <header class="lg:sticky lg:top-0 lg:flex lg:max-h-screen lg:w-1/2 lg:flex-col lg:justify-between lg:py-24 animate-slideInLeft">
  <!-- Perfil e Identificação -->
  <div class="flex flex-col items-center text-center lg:flex-row lg:items-start lg:text-left gap-4">
    <img src="/logo.png" alt="Foto de Thomas Eduardo R Nascimento" class="w-16 h-16 rounded-full shadow-lg hover:scale-105 transition-transform duration-300" />

    <div>
      <h1 class="text-4xl lg:text-5xl font-bold tracking-tight bg-clip-text text-transparent bg-gradient-to-b from-white to-neutral-400 animate-gradient">
        Thomas Eduardo
      </h1>

      <h2 class="mt-2 text-sm lg:text-lg font-medium text-neutral-400 animate-fadeIn delay-200">
        Desenvolvedor Web
      </h2>

      <!-- Botão de contato -->
    <a
  href="https://wa.me/551999042072"
  target="_blank"
  rel="noopener noreferrer"
  class="inline-flex items-center gap-x-2 mt-4 px-3 py-2.5 text-sm font-semibold text-white rounded-md border border-neutral-700 border-opacity-50 backdrop-blur-xl bg-neutral-400/10 shadow group transition-all hover:bg-green-400/20 hover:border-opacity-0"
>
  <span>Fale comigo no WhatsApp</span>
  <span class="w-2 h-2 rounded-full bg-green-400 animate-ping duration-1000"></span>
</a>

    </div>
  </div>

  <!-- Redes Sociais -->
  {SOCIALS.length > 0 && (
    <ul class="mt-8 flex justify-center lg:justify-start items-center gap-x-5 animate-fadeIn delay-500" aria-label="Redes sociais">
      {SOCIALS.map((social) => (
        <li
          class="p-2 text-xs rounded hover:bg-white/5 transition duration-300 transform hover:-translate-y-1"
          data-ccursor
          key={social.name}
        >
          <a
            href={social.url}
            target="_blank"
            rel="noreferrer noopener"
            class="block hover:text-slate-200 transition duration-200"
            aria-label={`${social.name} (abre em uma nova aba)`}
          >
            <span class="sr-only">{social.name}</span>
            {<Component component={social.icon} />}
          </a>
        </li>
      ))}
    </ul>
  )}
</header>


        <main id="content" class="pt-24 lg:w-1/2 lg:py-24 animate-slideInRight">
          <!-- SOBRE -->
         <section id="about" class="mb-16 scroll-mt-16 md:mb-24 lg:mb-36 lg:scroll-mt-24" aria-label="Sobre mim">
  <div class="mb-5 space-y-8 text-neutral-300">
    <p class="transition-opacity duration-1000 delay-200 animate-fadeIn">
      Sou desenvolvedor Fullstack com foco em interfaces modernas, acessíveis e responsivas. Já atuei em projetos completos, desde o front-end até o back-end, com integração de APIs, arquitetura de componentes e deploy em nuvem.
    </p>
    <p class="transition-opacity duration-1000 delay-300 animate-fadeIn">
      Tenho domínio de ferramentas como React, TypeScript, Node.js, Tailwind CSS, MongoDB e Docker. Prezo por qualidade de código, performance, experiência do usuário e aplico boas práticas como versionamento, deploy automatizado e segurança básica.
    </p>
  </div>
</section>


          <!-- EXPERIÊNCIA -->
          <section id="experience" class="mb-16 scroll-mt-16 md:mb-24 lg:mb-36 lg:scroll-mt-24" aria-label="Experiência de trabalho">
            <h2 class="text-lg font-bold uppercase tracking-widest text-white mb-6">Experiência</h2>
            <div>
              <ol class="group/list space-y-12">
                {EXPERIENCE.map((experience) => <Experience {...experience} />)}
              </ol>
            </div>
          </section>

     

          <!-- HABILIDADES -->
          <section id="skills" class="mb-16 scroll-mt-16 md:mb-24 lg:mb-36 lg:scroll-mt-24" aria-label="Habilidades">
            <h2 class="text-lg font-bold uppercase tracking-widest text-white mb-6">Habilidades</h2>
            <ul class="group/list space-y-4">
              {SKILLS.map((skill) => <Skill skill={skill} />)}
            </ul>
          </section>

          <!-- RODAPÉ FINAL -->
<footer class="w-full py-10 px-6 text-sm text-neutral-400  animate-fadeIn">
  <div class="max-w-4xl mx-auto flex flex-col sm:flex-row items-center justify-between gap-4">
    <div class="flex items-center gap-3">
      <img src="/favicon-preta.png" alt="Logo Thomas Eduardo" class="h-8 w-8 rounded-md shadow" />
      <span class="font-semibold text-neutral-200">Thomas Eduardo</span>
    </div>

    <nav class="flex items-center gap-4 text-xs sm:text-sm">
      <span class="text-neutral-400">📍 São Paulo - Brasil</span>
      <a href="https://www.thomasnascimento.online/" target="_blank" rel="noreferrer" class="hover:text-red-300 hover:underline transition-colors duration-200">Portfolio</a>
      <a href="https://www.linkedin.com/in/odevthomas/" target="_blank" rel="noreferrer" class="hover:text-blue-400 hover:underline transition-colors duration-200">@odevthomas</a>
    </nav>
  </div>
</footer>

        </main>
      </div>
    </div>
  </div>

  <footer class="text-center py-4 text-xs text-neutral-600 animate-fadeIn">
    {new Date().getFullYear()} Todos os direitos reservados.
  </footer>
</Layout>

<style scoped>
@keyframes fadeIn {
  0% {
    opacity: 0;
    transform: translateY(15px);
  }
  100% {
    opacity: 1;
    transform: translateY(0);
  }
}

@keyframes slideInLeft {
  from {
    opacity: 0;
    transform: translateX(-30px);
  }
  to {
    opacity: 1;
    transform: translateX(0);
  }
}

@keyframes slideInRight {
  from {
    opacity: 0;
    transform: translateX(30px);
  }
  to {
    opacity: 1;
    transform: translateX(0);
  }
}

@keyframes gradientFlow {
  0% {
    background-position: 0% 50%;
  }
  100% {
    background-position: 100% 50%;
  }
}

.animate-fadeIn {
  animation: fadeIn 1s ease forwards;
}

.animate-slideInLeft {
  animation: slideInLeft 1s ease-out forwards;
}

.animate-slideInRight {
  animation: slideInRight 1s ease-out forwards;
}

.animate-gradient {
  background-size: 200% 200%;
  animation: gradientFlow 5s ease infinite;
}

.hero {
  background-image: url(/pattern.svg);
  background-position: top center;
  background-repeat: no-repeat;
  overflow-y: visible;
  overflow-x: clip;
}
</style>
